# ============================================
# TECHZONE ENVIRONMENT CONFIGURATION
# ============================================

# === SERVER CONFIGURATION ===
PORT=5000
NODE_ENV=development

# === MONGODB CONFIGURATION ===
# Option 1: Local MongoDB (Recommended for development)
MONGODB_URI=mongodb://localhost:27017/TechZoneMongo

# Option 2: MongoDB Atlas (Uncomment when ready to use cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/TechZoneMongo?retryWrites=true&w=majority

# === MYSQL CONFIGURATION ===
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=techzone

# === SECURITY ===
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# === CORS ===
CORS_ORIGIN=http://localhost:5000,http://127.0.0.1:5000

# === RATE LIMITING ===
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# SETUP INSTRUCTIONS:
# ============================================
# 1. Copy this file to .env in your project root
# 2. Update MONGODB_URI if using Atlas
# 3. Update MYSQL credentials if using MySQL
# 4. Change JWT_SECRET to a random string
# 5. Never commit .env to git!
